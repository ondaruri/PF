<% if @notifications.exists? %>
<!--<%#= render 'notifications', visitor: notification.visitor, visited: notification.visited %>-->
<%= render 'notifications', notifications: @notifications %>
<% else %>
  <p>通知はありません</p>
<% end %>